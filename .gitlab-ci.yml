stages:
  - deploy

deploy_to_render:
  stage: deploy
  image: alpine:latest
  script:
    - apk add --no-cache curl
    - curl -X POST "$RENDER_DEPLOY_HOOK_URL"
  only:
    - main
    - master
  when: manual